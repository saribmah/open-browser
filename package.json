{
  "name": "open-browser",
  "type": "module",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "generate": "bun run --filter '*' generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
